import React from 'react';
import Button from '../UI/Button';
import Typography from '../UI/Typography';

export default function Home() {
  return (
    <div>
      <Button variant={'primary'} className="text-white">
        tesst
      </Button>
      <Typography component="h5">Home</Typography>
    </div>
  );
}
